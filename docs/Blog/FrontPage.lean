import VersoBlog
open Verso Genre Blog

#doc (Page) "Praxis" =>

*Praxis* is a practice ground for AI-assisted theorem proving in Lean 4.

This site is a proof-of-concept: machine-checked proofs rendered so you can hover
the tactics and watch the goal state evolve — the reasoning made visible, not just
the verdict. Every code example here compiled when the page was built.

See the posts for the first rendered proof.
